#include <iostream>
#include <cmath>

class point{
    int pstart;
    int pend;

};

class figuras{
    public:
        figuras();
        ~figuras();
        figuras(const figuras& other);

    protected:

    private:
};
