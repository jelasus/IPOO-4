#include <iostream>

template<typename T>
T min(T val1,T val2){
	if(val1<val2)
		return val1;
	else
		return val2;
}

#define min2(x,y) (x < y ? x : y)

static_cast <Triangle *> (pointer);
dynamic_cast <Triangle *> (pointer);